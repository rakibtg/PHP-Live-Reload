<?php

  // Set the interval of modified date in second.
  $env['interval'] = 1;

  // List of directories that will be ignored.
  $env['ignore'] = [
    'vendor',
    'node_modules',
    '.git',
    '.vscode',
    'docker_dev',
    'database/dumps',
    'cache',
    'storage/framework'
  ];

  // Set the host of live reloader.
  $env['host'] = '127.0.0.1';

  // Set the port of live reloader.
  $env['port'] = 2020;

  // PHP binary location path.
  $env['php'] = false;